'''
module06.py
'''

# import 이름
# print(이름.~)


# 1) numpy라는 패키지에서 random이라는 파일을 가져왔다.
# from numpy import random
# print(random.randint(1, 2)) #random이라는 이름으로 numpy를 가져왔다. 그러므로 random.~을 해줘야 실행됨


# 2) numpy라는 패키지 전체를 가져왔다.
import numpy
print(numpy.random.randint(1, 5)) # numpy 안 random이라는 파일까지 써주어야 실행됨
